---
description: Write the requested CNN tuning numerical-analysis section in the report using verified project evidence.
agent: build
---

Load and follow the project-local `cnn-tuning-report` skill. Write the
requested experiment or topic directly in `Report/Chapters/04_Tuning.tex`:

$ARGUMENTS

Do not return only a proposed LaTeX draft. Inspect the repository, use parallel
read-only evidence analysis where required by the skill, create only the
necessary figures or analysis scripts, and validate the report before replying.
